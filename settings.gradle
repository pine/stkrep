rootProject.name = 'stkrep'

include ':app'

for (File f : rootDir.listFiles()) {
    if (f.directory && f.name.startsWith('lib-')) {
        include(":${f.name}")
        project(":${f.name}").projectDir = f
    }
}
